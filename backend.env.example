# Exemplo de variáveis de ambiente para o BACKEND (não comitar)
# COLOQUE ESTAS CHAVES APENAS NO REPOSITÓRIO/AMBIENTE DO BACKEND (por exemplo no projeto do seu backend
# ou nas variáveis do serviço de hospedagem). Nunca exponha a service role key no frontend.

# URL do projeto Supabase
SUPABASE_URL=https://scsldapnrzpjkyqkeiop.supabase.co

# Service role key (SENSÍVEL) - colocar apenas no backend/servidor
SUPABASE_SERVICE_ROLE_KEY=<COLE_AQUI_SUA_SERVICE_ROLE_KEY>

# Stripe secret key (SENSÍVEL) - colocar apenas no backend/servidor
STRIPE_SECRET_KEY=<COLE_AQUI_SUA_STRIPE_SECRET_KEY>

# Opcional: chave anon (se quiser usar no backend sem privilégios extra)
SUPABASE_ANON_KEY=<OPCIONAL_ANON_KEY>

# Instruções rápidas:
# - Copie este arquivo para o backend e renomeie para .env (ou configure as variáveis no host).
# - Não comite arquivos com chaves sensíveis no Git. Use secrets/variables do provedor.
